#make copy database as R tables. TOIMII, mUT EI AJETA
# required_functions("save_db_table_to_RData")
# con <- connDB(con)
# tables <- dbFetch(dbSendQuery(con, "SHOW TABLES"))
# folder <- "./db_bu/"
# for(table in  tables[[1]]) {
#
#   save_db_table_to_RData(con, table, folder)
# }
